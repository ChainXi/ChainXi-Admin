chainxi:
  res:
    path: ${FILE_UPLOAD_PATH}
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
logging:
  level:
    com.baomidou.example.mapper: debug
management:
  endpoints:
    web:
      exposure:
        include: '*'